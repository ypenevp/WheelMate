import { Text, View } from 'react-native';
import "../global.css"

export default function Settings() {
    return (
        <View className="items-center justify-center mt-[100%]">
            <Text className="font-bold text-green-500">Settings Page</Text> 
        </View>
    );
}